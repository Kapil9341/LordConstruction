{
    "build":[{
        "src": "elevate/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLamdaSize": "15mb", "runtime": "Python 3.11.3"}
    }],
    
    "routes": [
        { "src": "/(.*)", "dest": "elevate/wsgi.py" }
      ]
   
}
